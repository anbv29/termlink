[package]
name = "termlink"
version = "0.1.0"
edition = "2024"
description = "A beginner-friendly terminal chat application"
license = "MIT"

[dependencies]
tokio = { version = "1.47", features = ["io-std", "io-util", "macros", "net", "rt-multi-thread"] }
